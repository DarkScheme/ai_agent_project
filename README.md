This is an ai agent project by boot.dev

It connects to Gemini AI and can execute functions (in functions folder) onto a directory (here calculator folder as an example)
It can read, write and run files in that directory

